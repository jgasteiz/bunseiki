[project]
name = "bunseiki"
version = "0.1.0"
description = "Japanese sentence generator for language learning"
requires-python = ">=3.13"
dependencies = [
    "streamlit",
    "pydantic-ai==1.0.15",
    "anthropic>=0.49.0",
    "python-dotenv==1.1.1",
    "watchdog",
]
